import React from 'react';

const Title = ({ text }) => {
  return (
    <div className="title-container">
      <h2 a href = '/' className="title">{'Countrys around the Globe'}</h2>
    </div>
  );
};

export default Title;